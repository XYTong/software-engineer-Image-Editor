class Tool():
    def manipulate(tool_name):
        if tool_name == 'Rotate':
            angle,direction = int(input())
            rotate = Rotate(self,angle,direction)
        elif tool_name = 'Resize':
            height,weight = int(input())
            resize = Resize(self,height,weight)
                    
        #.........to be continue

